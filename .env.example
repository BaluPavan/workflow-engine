# Local development values only. Copy to .env if you use a shell loader; never commit .env.
# The application has these same local defaults, so this file is optional for Docker Compose.
DB_USERNAME=workflow
DB_PASSWORD=workflow
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
